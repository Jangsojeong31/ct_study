package ct.week01;

public class Ex10_문자열_method {

	/*
	 * char charAt(index): 주어진 위치의 한 문자 리턴
	 * boolean startsWith(String prefix): 문자열이 prefix로 시작하면 true
	 * boolean endsWith(String suffix)
	 * boolean equals(Object obj): 문자열이 obj와 같다면 true
	 * int indexOf(x): 문자열에서 x 문자가 포함되어 있는 위치
	 * int lastIndexOf(String str): 문자열에서 str 문자열이 포함되어 있는 위치
	 * 
	 * int length(): 문자열의 길이 리턴
	 * String replace(char oldCar, char newChar): oldChar 문자를 newChar 문자로 교체
	 * String replaceAll(String regex, String replacement): regex 문자를 replacement 문자로 교체
	 * String substring(int beignIndex): 주어진 위치 이후의 문자열을 리턴
	 * String substring(int beginIndex, int endIndex): beginIndex에서 endIndex까지의 문자열 리턴
	 * boolean contains(CharSequence chars): chars 문자열이 포함되어 있는지 확인 
	 */
}
